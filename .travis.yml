language: cpp

compiler:
  - clang

install:
  sudo apt-cache search libboost

script:
  mkdir debug &&
  cd debug &&
  cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_COMPILER=clang++ .. &&
  make check

